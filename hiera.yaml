---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: 'OS family and architecture'
    path: 'os/%{facts.os.family}/%{facts.os.architecture}.yaml'
  - name: 'OS family'
    path: 'os/%{facts.os.family}.yaml'
  - name: 'common'
    path: 'common.yaml'
